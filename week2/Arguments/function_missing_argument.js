function findAverage(firstNumber, secondNumber, thirdNumber){
    let summation = firstNumber + secondNumber + thirdNumber;
    let average = summation / 3;
    return average;
}

let functionResult = findAverage(2,2);
console.log(functionResult);
